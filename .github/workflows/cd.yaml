name: CD

on:
  pull_request:
    branches: [ main ]
    paths-ignore:
      - 'docs/**'

jobs:
  checkout:
    uses: actions/checkout@v2
  deploy:
    uses: MinimumViableDataspace/MinimumViableDataspace/.github/workflows/deploy.yml
